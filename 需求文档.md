# 智能网页工作便签（个人云端版）功能需求文档（简版）

> 目标：把现有“网页便签”原型升级为**可部署到服务器**、可在任意设备登录使用的个人工具。
>
> 说明：本项目**仅本人单用户使用**，不做多用户体系与复杂权限；但需要“登录/鉴权”以避免他人访问。

---

## 1. 必做功能（一期）

### 1.1 登录与访问控制（单用户）

* **访问必须先登录**：未登录不能看到任何便签内容。
* **单用户账号**：只有一个账号（管理员/本人）。
* 登录方式（二选一，由乙方实现其一即可）：

  1. 账号密码登录（推荐）
  2. 访问口令/密码（输入一次后保持登录）
* **登录态保持**：关闭浏览器/刷新后仍保持登录（可配置 7/14/30 天）。
* **退出登录**：清除登录态。
* **修改密码/口令**：在设置中可修改。
* **防暴力尝试**：连续失败登录需短暂限制（例如 5 次失败后 5 分钟）。

> 备注：因为是个人使用，不要求注册/找回密码流程，但必须能修改密码。

---

### 1.2 便签核心（增删改查）

* 新建便签
* 编辑便签
* 删除便签（需二次确认）
* 便签字段：

  * 标题（必填）
  * 内容（必填）
  * 内容格式：纯文本 / Markdown
  * 标签：支持多个，逗号分隔输入
  * 颜色：可选
  * 完成状态：已完成 / 未完成
  * 创建时间、更新时间（自动生成）

#### 完成状态

* 单条便签可一键“完成/取消完成”
* 支持“一键完成所有”（保留原型的快捷按钮）
* 已完成便签需要明显样式变化（置灰/删除线等，沿用原型）

---

### 1.3 列表能力（搜索 / 排序 / 筛选 / 分页）

#### 搜索

* 搜索范围：标题、内容、标签
* 支持即时搜索（输入即过滤）

#### 排序

* 按日期倒序（默认）
* 按日期正序
* 按标题 A-Z
* 按标题 Z-A
* 按完成状态

#### 筛选

* 全部
* 未完成
* 已完成
* 重要
* 今天（今天创建的便签）

> 重要的判定：可以沿用“标签包含‘重要’”的方式；也允许增加 isImportant 字段（乙方可选）。

#### 分页

* 每页显示数量：6 / 9 / 12 / 15（与原型一致）
* 分页控件显示与跳转

---

### 1.4 Markdown 支持

* 当格式为 Markdown：

  * Markdown 渲染展示
  * 代码块语法高亮
  * 支持基本元素：标题、列表、引用、表格、链接、图片
* 当格式为纯文本：

  * 纯文本原样显示

---

### 1.5 主题与界面（尽量沿用原型）

* 主题选择：保留现有多主题（紫/蓝/绿/暗色等）
* 主题选择记忆：下次打开保持上次主题
* 浮动按钮：

  * 快速添加
  * 设置
  * 主题切换
  * 一键完成所有

> UI 不要求 100% 像素级复刻，但交互结构、主要布局与功能入口需保持一致。

---

### 1.6 数据导入 / 导出

#### 导出

* 导出全部便签为 JSON 文件
* 文件名包含时间戳（如 notes_YYYYMMDD_HHMMSS.json）

#### 导入

* 支持两种导入方式：

  1. 选择 JSON 文件导入
  2. 粘贴 JSON 文本导入
* 导入校验：

  * JSON 格式合法
  * 字段合法（标题/内容不能为空，长度在允许范围内）
* 导入策略（需提供选择）：

  * 合并导入：保留现有便签并新增
  * 覆盖导入：清空现有便签后导入

---

### 1.7 设置

* 修改登录密码/口令
* 自动备份设置（见 1.8，可做开关）
* 导出格式（一期至少 JSON；TXT 可选）

---

### 1.8 备份与防丢（强烈建议）

因为是个人数据，核心是“别丢”。

一期至少实现其中一种：

* **服务器端定时备份**：每天自动备份一次到服务器目录（保留最近 N 天，如 7 天）
* **一键备份**：点击按钮生成备份文件（JSON）

> 原型里的“定时导出到浏览器下载”可保留为可选功能，但服务器端备份更可靠。

---

## 2. 部署与访问（简单要求）

* 可部署到 Linux 服务器
* 提供一个可访问地址（域名或 IP）
* 支持 HTTPS（建议；如乙方不做，至少说明如何配合 Nginx/反代实现）
* 提供最简部署说明：

  * 如何启动/停止
  * 如何配置端口
  * 数据存储位置
  * 如何备份/恢复

---

## 3. 数据与安全（以“防别人看到”为核心）

* 未登录不可访问便签列表与详情
* 接口必须鉴权（不允许直接 URL 访问拿到数据）
* 登录密码不可明文存储
* 建议增加：

  * 自动登出：长时间未操作可要求重新登录（可选）
  * 操作日志（可选）：记录最后登录时间/最后备份时间

---

## 4. 页面/模块清单（简版）

* 登录页（或登录弹窗）
* 主页面（便签列表 + 搜索/筛选/排序/分页 + 操作按钮）
* 新建/编辑便签弹窗
* 导入弹窗
* 设置弹窗

---

## 5. 验收清单（面向甲方可测试）

说明：以下每条都必须现场演示通过。

1. 未登录访问主页面：必须跳转/显示登录
2. 登录后：可正常查看、新建、编辑、删除便签
3. 完成/未完成切换正确；“一键完成所有”可用
4. 搜索/排序/筛选/分页全部可用
5. Markdown 渲染与代码高亮可用
6. 导出 JSON 可用；导入 JSON 可用（合并/覆盖策略生效）
7. 服务器部署后：换电脑/手机登录仍能看到同一份数据
8. 备份机制可用（定时备份或一键备份至少一种）

---

## 6. 二期可选（不要求一期实现）

* 回收站（误删可恢复）
* 归档/文件夹
* 全局快捷键
* 离线模式
* AI 辅助整理/总结

---

